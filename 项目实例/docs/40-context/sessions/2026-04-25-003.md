# Session 2026-04-25-003

## 任务
UI 精简与全屏布局优化：移除 Header、移动配置按钮、清除组件内部重复标题

## 完成的工作

### 1. 移除顶部 Header，Dashboard 占满全屏
- **文件**: `src/frontend/src/App.vue`
- **变更**:
  - 移除 `<header>` 元素（包含标题、副标题、配置按钮）
  - 直接渲染 `<Dashboard />` 组件
  - 使用 `provide` 将配置模态框显示函数传递给子组件

### 2. 配置按钮移到控制面板右上角并缩小
- **文件**: `src/frontend/src/components/MinerControl.vue`
- **变更**:
  - 使用 `inject` 接收 `showConfig` 函数
  - 在控制面板右上角添加 ⚙️ 按钮（`config-btn` 样式）
  - 按钮缩小：`padding: 4px 8px; font-size: 12px`

### 3. 移除 MinerTerminal 内部标题
- **文件**: `src/frontend/src/components/MinerTerminal.vue`
- **变更**:
  - 移除 `<div class="terminal-header">`（包含 `<h3>实时终端</h3>` 和清空按钮）
  - 终端内容区占满整个组件高度
  - `.miner-terminal` 添加 `height: 100%; display: flex; flex-direction: column`
  - `.terminal-content` 添加 `flex: 1`

### 4. 移除 MinerControl 内部标题
- **文件**: `src/frontend/src/components/MinerControl.vue`
- **变更**:
  - 移除 `<h3>控制面板</h3>` 标题
  - 保留状态徽章和配置按钮在同一行

### 5. Dashboard 高度调整为全屏
- **文件**: `src/frontend/src/views/Dashboard.vue`
- **变更**:
  - `.dashboard-content` 高度从 `calc(100vh - 80px)` 改为 `100vh`

## 代码变更

| 文件 | 变更类型 | 说明 |
|------|---------|------|
| `src/frontend/src/App.vue` | 修改 | 移除 header，使用 provide 传递 showConfig |
| `src/frontend/src/components/MinerControl.vue` | 修改 | 移除 h3 标题，添加右上角 ⚙️ 配置按钮 |
| `src/frontend/src/components/MinerTerminal.vue` | 修改 | 移除 terminal-header，内容区占满全高 |
| `src/frontend/src/views/Dashboard.vue` | 修改 | dashboard-content 高度改为 100vh |

## 遇到的阻塞
- 无

## 关键决策
- 使用 Vue 的 `provide/inject` 机制传递配置按钮的显示函数，避免 props drilling
- 组件内部标题通过 TabContainer 的标签栏统一显示，避免重复

## 下一步
- 验证浏览器中全屏布局效果
- 测试配置按钮点击是否正常弹出模态框
- 测试控制面板、实时终端、已发现密钥三个标签的切换功能

## 状态同步检查
- [x] 已更新 `active-context.md`
- [x] 已更新 `changelog.md`
- [x] 已更新本文件到 `sessions/2026-04-25-003.md`
